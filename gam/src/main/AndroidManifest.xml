<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aotter.trek.gam.mediation.demo">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.aotter.trek"
        tools:targetApi="s">

        <meta-data
            android:name="trek.client.id"
            android:value="DNgNhOwfbUkOqcQFI+uD" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-6517778919326488~1622832617" />

        <activity android:name=".native_ad.GamNativeAdRecyclerViewPageActivity" />
        <activity android:name=".native_ad.GamNativeAdScrollViewActivity" />
        <activity android:name=".banner_ad.GamBannerAdScrollViewActivity" />

        <activity
            android:name=".GamActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>